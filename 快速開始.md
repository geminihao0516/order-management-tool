# 🚀 快速開始指南

## 📋 目錄
- [本地運行](#本地運行)
- [部署到 GitHub + Streamlit Cloud](#部署到雲端)
- [測試功能](#測試功能)

---

## 本地運行

### 步驟 1：確認環境

檢查 Python 是否已安裝：
```bash
python3 --version
```

應該顯示 Python 3.7 或更高版本

### 步驟 2：安裝相依套件

```bash
cd "/Users/hao/Desktop/訂單整理工具"
pip3 install -r requirements.txt
```

### 步驟 3：啟動應用

**方式 A：命令列**
```bash
streamlit run app.py
```

**方式 B：雙擊啟動（macOS）**
- 找到 `啟動網頁版.command`
- 雙擊執行

### 步驟 4：開啟瀏覽器

- 瀏覽器會自動開啟
- 或手動訪問：http://localhost:8501

---

## 部署到雲端

### 前置需求

1. ✅ GitHub 帳號（[註冊](https://github.com/signup)）
2. ✅ Git 已安裝（macOS 內建）

### 步驟 1：初始化 Git

```bash
cd "/Users/hao/Desktop/訂單整理工具"

# 初始化 Git
git init

# 添加所有文件
git add .

# 創建第一次提交
git commit -m "Initial commit: 訂單整理工具網頁版"
```

### 步驟 2：建立 GitHub Repository

1. 登入 [GitHub](https://github.com)
2. 點擊右上角 `+` → `New repository`
3. 填寫資訊：
   - **Repository name**: `order-management-tool`
   - **Description**: 訂單資料整理工具
   - **Public**: ✅ 選擇 Public（免費部署必須）
   - **不要勾選** Initialize this repository with...
4. 點擊 `Create repository`

### 步驟 3：推送程式碼

GitHub 會顯示指令，複製並執行（記得替換成您的用戶名）：

```bash
git remote add origin https://github.com/你的用戶名/order-management-tool.git
git branch -M main
git push -u origin main
```

如果需要登入：
- Username: 您的 GitHub 用戶名
- Password: **使用 Personal Access Token**（不是密碼）
  - 建立 Token：GitHub → Settings → Developer settings → Personal access tokens → Generate new token

### 步驟 4：部署到 Streamlit Cloud

1. 訪問 [streamlit.io/cloud](https://streamlit.io/cloud)

2. 點擊 `Sign in with GitHub` 登入

3. 點擊 `New app`

4. 填寫部署資訊：
   - **Repository**: 選擇 `你的用戶名/order-management-tool`
   - **Branch**: `main`
   - **Main file path**: `app.py`

5. 點擊 `Deploy!`

6. 等待 2-5 分鐘，部署完成！

### 步驟 5：獲取網址

部署成功後，您會看到：
```
🎉 Your app is live at:
https://你的用戶名-order-management-tool-app-xxxxx.streamlit.app
```

**保存這個網址**，可以分享給任何人使用！

---

## 測試功能

### 基本測試流程

1. **開啟應用**（本地或雲端網址）

2. **輸入測試資料**
   - 點擊「📄 範例資料」
   - 或點擊「📋 貼上範例資料」

3. **生成報表**
   - 點擊「🚀 生成報表」

4. **查看結果**
   - 切換到「📊 報表結果」頁籤
   - 查看統計數據
   - 下載報表

### 測試案例 1：單一品項

```
招財女神x1	李丞宣 1975/2/25	—	願望：偏財天天來
```

### 測試案例 2：多個品項

```
鬼王x2+三鬼頭x4	王小明 1990/5/20	李美麗 1992/8/15	願望：事業順利
愛神x1	陳大華 1985/12/10	—	求好姻緣
```

### 測試案例 3：參考數據比對

**訂單資料：**
```
鬼王x2	王小明 1990/5/20	李美麗 1992/8/15	事業順利
三鬼頭x3	陳大華 1985/12/10	—	求好姻緣
```

**參考數據：**
```
2支鬼王、3支三鬼頭
```

---

## 常見問題

### Q: 本地運行時出現 "ModuleNotFoundError: No module named 'streamlit'"

**A:** 執行安裝命令：
```bash
pip3 install streamlit
```

### Q: Git push 時要求密碼但失敗

**A:** GitHub 已不支援密碼登入，需使用 Personal Access Token：

1. GitHub → Settings → Developer settings
2. Personal access tokens → Tokens (classic) → Generate new token
3. 勾選 `repo` 權限
4. 複製 token（只會顯示一次！）
5. 推送時用 token 作為密碼

### Q: Streamlit Cloud 部署失敗

**A:** 檢查：
1. Repository 是否為 Public
2. `requirements.txt` 是否存在
3. `app.py` 是否在根目錄
4. 查看 Streamlit Cloud 的部署日誌

### Q: 如何更新已部署的應用

**A:** 只需推送新的程式碼：
```bash
git add .
git commit -m "更新說明"
git push
```
Streamlit Cloud 會自動重新部署（約 2 分鐘）

### Q: 可以使用自己的網域名稱嗎？

**A:** 可以，但需要：
1. Streamlit Cloud 付費方案
2. 或使用反向代理（較複雜）

---

## 進階配置

### 自訂主題

建立 `.streamlit/config.toml`：

```toml
[theme]
primaryColor = "#1f77b4"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f0f2f6"
textColor = "#262730"
font = "sans serif"

[server]
maxUploadSize = 200
```

### 設定 Secrets

如果需要隱藏敏感資訊（如 API Keys）：

1. Streamlit Cloud → 您的 App → Settings → Secrets
2. 添加：
   ```toml
   API_KEY = "your-secret-key"
   ```
3. 在程式中使用：
   ```python
   import streamlit as st
   api_key = st.secrets["API_KEY"]
   ```

---

## 效能優化

### 使用快取

```python
import streamlit as st

@st.cache_data
def load_data():
    # 耗時的資料載入
    return data
```

### 減少重新執行

使用 `st.session_state` 儲存狀態，避免重複計算

---

## 需要幫助？

- 📖 [Streamlit 文檔](https://docs.streamlit.io)
- 💬 [Streamlit 社群](https://discuss.streamlit.io)
- 🐛 [回報問題](https://github.com/你的用戶名/order-management-tool/issues)

---

## 下一步

✅ 本地測試成功
✅ 部署到雲端成功
🎉 開始使用！

**分享您的應用：**
```
https://你的用戶名-order-management-tool-app-xxxxx.streamlit.app
```

**更新 README.md**，將上面的網址填入 README 的徽章中！
